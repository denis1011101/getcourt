# See https://www.robotstxt.org/robotstxt.html for documentation on how to use the robots.txt file

User-agent: *
Disallow:

# /locale/:locale нарочно не закрыт: это редирект на языковой поддомен, и пока он
# стоял в Disallow, Google не мог его обойти, чтобы увидеть редирект, и держал
# такие адреса в индексе («Indexed, though blocked by robots.txt»).

# Бесконечная лента: у каждой страницы свой курсор, он живёт пару часов, а потом
# отвечает редиректом на /tennis_life. Для краулера это бесконечное пространство
# адресов с нулевой пользой — карточки те же, что и на самой странице ленты.
Disallow: /tennis_life/feed

# Multilingual sitemap
Sitemap: https://getcourt.co/sitemap.xml
